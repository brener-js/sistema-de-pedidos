name: CI/CD Pipeline - Sistema de Pedidos (SGP)

on:
  push:
    branches:
      - main
  pull_request:
    branches:
      - main

jobs:
  test-and-build:
    name: Backend QA & Verification
    runs-on: ubuntu-latest
    
    steps:
      - name: Checkout Repository
        uses: actions/checkout@v3

      - name: Setup Node.js Environment
        uses: actions/setup-node@v3
        with:
          node-version: '18'
          cache: 'npm'
          cache-dependency-path: './backend/package-lock.json'

      - name: Install Backend Dependencies
        working-directory: ./backend
        run: npm ci

      - name: Run Backend Integration Tests (Jest)
        working-directory: ./backend
        run: npm test

  # webhook-deploy:
  #   name: Trigger Render & Vercel Deploy Hook
  #   needs: test-and-build
  #   runs-on: ubuntu-latest
  #   if: github.ref == 'refs/heads/main'
  #   steps:
  #     - name: Trigger Backend Deploy on Render
  #       run: curl -s -X POST ${{ secrets.RENDER_DEPLOY_HOOK }}
  #     - name: Trigger Frontend Deploy on Vercel
  #       run: curl -s -X POST ${{ secrets.VERCEL_DEPLOY_HOOK }}
